services:
    postgres:
        image: postgres:18
        container_name: jat
        ports:
            - '5432:5432'
        environment:
            POSTGRES_USER: postgres
            POSTGRES_PASSWORD: 123123
            POSTGRES_DB: jat
        volumes:
            - postgres_data:/var/lib/postgresql
volumes:
    postgres_data:
